### See this app live
To see live this app go to https://stripe-server-checkout-example.herokuapp.com/

### Run this app locally
This app uses Ruby and Sinatra

1. Replace your secret key in `/server.rb` and your public key in `/public/assets/js/checkout.js`

2. Open the terminal and go to the root directory and run 
`ruby server.rb`
